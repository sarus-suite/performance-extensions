{
  "name": "performance-extensions glibc builder",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "../..",
    "args": {
      "USERNAME": "vscode",
      "USER_UID": "1000",
      "USER_GID": "1000"
    }
  },
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}"
}
